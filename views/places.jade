extends layout

block content
	h1 Enter the user place
	form(name="placedata", action="/places", method="post")
		input(type="text", name="user_id")
		input(type="number", name="latitude")
		input(type="number", name="longitude")
		input(type="submit", value="submit")
